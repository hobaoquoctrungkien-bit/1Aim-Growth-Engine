Do not run Git backup automatically after each task.

Only run git add, git commit, or git push when the user explicitly asks for Git backup, commit, or push.

Never claim Git updated unless push succeeded.

After every completed task, run:

powershell -ExecutionPolicy Bypass -File scripts/notify_done.ps1
